import ScheduleEntryForm from "@/components/admin/ScheduleEntryForm";

export const metadata = { title: "New race — Shop Admin" };

export default function NewSchedulePage() {
  return <ScheduleEntryForm />;
}
